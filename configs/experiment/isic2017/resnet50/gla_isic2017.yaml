# @package _global_
training_type: standard

sparsity_lambda: 1.0e-05
sparsity_type: group_lasso-activation

learning_rate: 0.00005

model: resnet50
resnet:
  norm_layer: null # "none"
  weights: "imagenet"

use_class_weights: true
class_weight_method: "balanced"

wandb:
  tag: "isic2017-gla"